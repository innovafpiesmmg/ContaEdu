# ContaEdu - Variables de Entorno
# Copia este archivo como .env y rellena los valores

# Base de Datos PostgreSQL
DATABASE_URL=postgresql://contaedu:TU_PASSWORD@localhost:5432/contaedu_db

# Secreto para sesiones (genera uno seguro con: openssl rand -base64 32)
SESSION_SECRET=cambia-esto-por-un-secreto-seguro

# Entorno
NODE_ENV=production

# Puerto (el script de instalación usa 5000 por defecto)
PORT=5000

# Dominio de la aplicación (usado para enlaces de recuperación de contraseña)
# Solo el nombre de host, SIN protocolo (sin https://)
# Ejemplo: contaedu.micentro.es
# Si se deja vacío, se usará el host de la petición HTTP
APP_DOMAIN=

# Forzar HTTP (true si no hay dominio/SSL, false si hay dominio con HTTPS)
FORCE_HTTP=true
